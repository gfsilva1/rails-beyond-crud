<%= form_with model: @propriedade, url: proprietario_propriedades_path do |f| %>
  <%= f.label :nome  %>
  <%= f.text_field :nome %>
  <%= f.label :id %>
  <%= f.text_field :proprietario_id %>
  <%= f.submit %>
<% end %>
